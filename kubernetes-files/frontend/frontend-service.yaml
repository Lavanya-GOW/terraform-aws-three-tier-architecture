apiVersion: v1
kind: Service

metadata:
  name: frontend-service
  namespace: three-tier-app
  labels:
    app: flask

spec:
  selector:
    app: flask

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000

  type: ClusterIP